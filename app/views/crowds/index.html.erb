<% if current_user.nil? or current_user.crowds.size == 0 %>
	<p>
		<%= AppName %> takes any number of sources - blogs, news sites, Twitter streams etc - and tracks their RSS/Atom feeds for recurring links. 
		If a link to a page appears in two or more of your sources, or if one source links to an item in another source, that link is considered interesting 
		and is added to the "Items" list.
	</p>
<% end %>

<% if current_user.nil? %>
	<p>
		<%= link_to "Sign in", "/signin" %> with OpenID to start creating your own crowd, or take a look at a sample crowd's 
		<%= link_to '<b>sources</b>', crowd_feeds_path(1) %> and <%= link_to '<b>current items</b>', crowd_items_path(1) %>.
	</p>

<% else -%>
	<% if current_user.crowds.size == 0 -%>
		<p>
			Welcome! To start off, name your crowd:<br />
		</p>
	<% else -%>
		<p>
			Your crowds: 
			<% for crowd in @crowds -%>
			<li>
				<%= link_to crowd.title, crowd_items_path(crowd) -%>&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;
				[<%= link_to "Delete", crowd_path(crowd), :method => :delete -%>]
			</li>
			<% end -%>
		</p>

	<% end -%>

		<p>&nbsp;</p>

		<p>
		<% form_for(@crowd.nil? ? Crowd.new : @crowd) do |f| %>
			<span style="color: green"><%= @crowd.errors['title'] unless @crowd.nil? %></span>
			<%= f.text_field :title %>
			<%= f.submit "Create New Crowd" %>
		<% end -%>
	</p>
	
<% end -%>