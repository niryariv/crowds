
* disable delete/entry for unauthorized users (or handle it cleanly)
* 1 crowd per user?

* remove unused views/controllers


#acts_as_auth -> AuthLogic migration:
- what was 'salt' is now 'password_salt'

* clean old tags (SellByDate = Gaps.max ??)

* undo messages

* hover bg color/delete icon for feeds

* let non-logged in users see feeds/items

* DB migrations

* RoR 2.x -> allow .format
